import 'package:flutter/material.dart';

class EnquetesEncerradas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Enquetes Encerradas'),
      ),
    );
  }
}
