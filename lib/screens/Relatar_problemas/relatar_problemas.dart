import 'package:flutter/material.dart';

class RelProblemsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Relatar Problemas'),
      ),
    );
  }
}
